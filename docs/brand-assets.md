# Nestd logo provenance

The landing page uses the existing `logo.png` without redrawing, recolouring, cropping or otherwise modifying the artwork.

- Landing source: `/Users/hichamsadike/nestdAI/nestd-landing/logo.png` (512 × 512 RGBA PNG), already used by the original site's header, hero and footer.
- App counterpart: `/Users/hichamsadike/nestdAI/nestd/app/assets/icon.png` (1024 × 1024 RGBA PNG), configured as the app icon in `/Users/hichamsadike/nestdAI/nestd/app/app.json`.
- Touch icon: the existing `apple-touch-icon.png` (180 × 180) remains available for browser use.

Both main assets show the same pink/red house, nest and rising arrow on a white rounded-square tile. The tile is part of the artwork; only its outer padding is transparent. In `logo.png`, the non-transparent bounds are x52–459 and y52–460, so the visible tile occupies about 80% of the image dimensions. Display the whole image with its original aspect ratio and allow for this padding when sizing it.

No separate official wordmark or logo SVG was found. The Android file `app/assets/android-icon-foreground.png` contains a blue Expo template symbol and must not be substituted for the Nestd mark.
