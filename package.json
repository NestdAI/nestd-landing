{
  "name": "nestd-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs && node scripts/build-utility-pages.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "preview": "node scripts/serve.mjs",
    "build:vercel": "npm run build && node scripts/package-site.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.55.1",
    "prettier": "3.6.2",
    "qrcode": "1.5.4"
  }
}
